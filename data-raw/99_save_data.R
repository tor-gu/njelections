# Save the tables

usethis::use_data(election_by_municipality, overwrite = OVERWRITE_DATA)
usethis::use_data(election_by_county, overwrite = OVERWRITE_DATA)
usethis::use_data(election_statewide, overwrite = OVERWRITE_DATA)
